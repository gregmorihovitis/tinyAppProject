<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <h2>URLS:</h2>
      <ul>
        <% urls.forEach(function(url) { %>
          <li><%= url.shortened %> = <%= url.original %></li>
        <% }); %>
      </ul>
    <h3 href = '#'>Shorten a URL</h3>
</body>
</html>